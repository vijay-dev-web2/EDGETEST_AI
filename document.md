You are the core engineering intelligence for EdgeTest AI Assistant. You need to update your internal knowledge base, fix an active fallback routing bug, and resolve a front-end theme state sync issue.&nbsp;Execute the following system configurations immediately:---### 1. ASSISTANT KNOWLEDGE BASE UPDATE (Supported Languages)When a user asks "what are the languages i can test here" or any variation regarding language support, you must answer with absolute certainty.&nbsp;- Core Support: Python, JavaScript, and TypeScript.- Frame Capabilities: You natively support automated unit and integration test generation using PyTest (Python), Jest/Mocha (JavaScript), and Vitest/Jest (TypeScript).- Never trigger fallback loops like "I'm not fully sure about that yet" for language capability queries.---### 2. CORE CAPABILITY ALIGNMENTAlign your interactive capability guide with the recently upgraded backend engine architecture:- Risk Score: Derived from code complexity, guard clause density, and logical branch depth.- Traceability Map: Maps 100% of discovered logical branches (Happy Path, Negative, Boundary, and Exception) to generated test methods.- Sandbox Execution: Isolated execution runtimes that execute tests securely to verify compilation and prevent environment contamination.---### 3. DARK / LIGHT THEME TOGGLE BUGFIX (UI State Remediation)The frontend theme toggle feature is currently failing to switch styles. Inspect the web client layout components and apply the following technical fix:- State Binding: Ensure the toggle switch modifies the `data-theme` attribute on the root HTML/body document level (e.g., `document.documentElement.setAttribute('data-theme', 'dark')`).- Tailwind/CSS Variable Mapping: Verify that CSS variables (`--background`, `--text`, etc.) are correctly mapped inside the Tailwind configuration using `light:` or `dark:` variants, or basic utility overrides.- Persistence Layer: Verify that the selected theme state is committed to `localStorage.setItem('theme', 'dark'|'light')` and read immediately during the initial DOM render event to prevent layout flashing (FOUC).---### OUTPUT EXPECTATIONReview the repository files for the chatbot UI component and the assistant's NLP intent handler (`discovery.py` or your designated assistant router). Implement the missing language intent routing, update the user response templates, and fix the JavaScript/CSS theme listener loop. Return a clean git diff showing the completed fixes.